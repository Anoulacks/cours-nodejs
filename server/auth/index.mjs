/*
const auth = (name, password) => {
    const file = fs.readFileSync('./controllers/auth/users.json', 'utf8')
    const users = JSON.parse(file)
    const user = users.fins((e) => e.name === name && e.password === password)

    return generateAccessToken(user)
}

export { auth }
*/